# Simple Chat

## How to start

In the project directory, you need to run:
#### `npm install`

After downloading all node modules you need to run server:
#### `node server`

Then run react app:
#### `npm start`